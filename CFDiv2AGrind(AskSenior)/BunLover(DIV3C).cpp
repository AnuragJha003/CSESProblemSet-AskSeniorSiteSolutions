#include <bits/stdc++.h>
#define int long long
using namespace std;

int32_t main() {
    int t(1);
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        cout << ((n + 1) * n) + n + 2 << endl;
    }
    return 0;
}